#this is a calculator that calculates area of a rectangle

#the user inputs the length
length = float(input("what is the length? : "))

#the user inputs the width
width = float(input("what is the width? : "))

#the code multiplies the length and width
area = length * width

# the area is printed in the terminal
print(f"the area is : {area}")
